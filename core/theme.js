import {DefaultTheme} from 'react-native-paper'

export const theme = {
    ...DefaultTheme,
    roundness: 10,
    colors: {
        ...DefaultTheme.colors,
        text: '#000000',
        primary: '#58c430',
        secondary: '#414757',
        error: '#f13a59',
    },
}
